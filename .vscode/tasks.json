{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start-app",
            "type": "shell",
            "command": "chmod +x bis.sh && ./bis.sh",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
